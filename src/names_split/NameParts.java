package names_split;

public class NameParts { // BUG
/*

  public void writeNameParts(String fuldeNavn) {

    // Finde fornavn. Fornavn er fra index til firstSpace
    int indexFornavn = fuldeNavn.indexOf(fuldeNavn);
    String fornavn = fuldeNavn.substring(indexFornavn, fuldeNavn.indexOf(" "));

    // Finde mellemnavn. Mellemnavn er fra firstSpace+1 til lastSpace
    int indexMellemnavn = fuldeNavn.indexOf(" ")+1;
    int slutMellemnavn = fuldeNavn.lastIndexOf(" ");
    String mellemnavn = fuldeNavn.substring(indexMellemnavn, slutMellemnavn);

    // Finde efternavn. Efternavn er fra lastSpace
    int indexStartEfternavn = fuldeNavn.lastIndexOf(" ")+1;
    String efternavn = fuldeNavn.substring(indexStartEfternavn);
  }

  public static void main(String[] args) {

    NameParts name = new NameParts();

    name.writeNameParts("Peter Heronimous Lind");
    name.writeNameParts("Harry James Potter");
    name.writeNameParts("Albus Percival Dumbledore");
    name.writeNameParts("James Gosling");

  }

 */
}
